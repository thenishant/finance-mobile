import {colors} from "./colors";

export const transactionColors = {
    INCOME: {
        primary: colors.green,
    },
    EXPENSE: {
        primary: colors.red,
    },
    INVESTMENT: {
        primary: colors.blue,
    },
    TRANSFER: {
        primary: colors.orange,
    },
};